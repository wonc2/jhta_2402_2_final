<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.jhta_2402_2_final.dao.product.ProductCompanyDao">
    <select id="getSourcesByCompanyName" resultType="Map" parameterType="String">
        select
            sp.SOURCE_PRICE_ID as 'companySourceId',
            s.NAME as 'sourceName',
            PRICE as 'sourcePrice',
            pc.NAME as 'companyName',
            pc.ADDRESS as 'companyAddress',
            pc.product_company_ID as companyId
        from SOURCE_PRICE sp
        join SOURCE s ON s.source_id = sp.source_id
        join PRODUCT_COMPANY pc ON pc.product_company_id = sp.product_company_id
        <if test="companyName != null and companyName != ''">
            where pc.NAME = #{companyName}
        </if>
    </select>

    <insert id="insertCompanySource" parameterType="Map">
        INSERT INTO
            SOURCE_PRICE (SOURCE_PRICE_ID, PRODUCT_COMPANY_ID, SOURCE_ID, PRICE)
        VALUES
            (UUID(), #{companyId}, #{sourceId}, #{sourcePrice})
    </insert>

    <insert id="addSource" parameterType="String">
        INSERT INTO
            SOURCE (SOURCE_ID, NAME)
        VALUES
            (UUID(), #{sourceName})
    </insert>


    <select id="getCompanyIdByName" parameterType="String" resultType="String">
        SELECT PRODUCT_COMPANY_ID as 'companyId' FROM PRODUCT_COMPANY WHERE NAME = #{companyName}
    </select>
    <select id="getSourceIdByName" parameterType="String" resultType="String">
        SELECT SOURCE_ID as 'sourceId' FROM SOURCE WHERE NAME = #{sourceName}
    </select>
    <select id="getAllSources" resultType="SourceDto">
        SELECT SOURCE_ID as 'sourceId', NAME as 'sourceName' FROM SOURCE
    </select>

    <select id="duplicationSource" resultType="int" parameterType="String">
        SELECT COUNT(*) FROM SOURCE WHERE NAME = #{sourceName}
    </select>
</mapper>